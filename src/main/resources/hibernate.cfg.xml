<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Conexión a la base de datos -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/tallerxpert?serverTimezone=UTC&amp;useSSL=false</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">123456</property>

        <!-- Dialecto de MySQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Mostrar las consultas SQL en consola -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>         <!-- formatea la salida SQL -->
        <property name="hibernate.use_sql_comments">true</property>   <!-- agrega comentarios sobre la acción -->

        <!-- Actualizar automáticamente la estructura de la BD -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Registrar aquí las clases mapeadas -->
        <mapping class="modelo.Cliente"/>
    </session-factory>
</hibernate-configuration>